server:
  port: 8030
spring:
  application:
    name: demo
  datasource:
    url: jdbc:mysql://localhost:3306/users_service
    username: root
    password: admin
  jpa:
    hibernate:
      ddl-auto: update

  data: # Configuración MongoDB
    mongodb:
      host: localhost
      port: 27017
      database: product_service

logging:
  level:
    root: debug